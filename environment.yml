name: hfradarpy
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10.2
  - xarray
  - netcdf4
  # - bottleneck [--no-deps]
  # - dask [--no-deps]
  - geopandas
  - cartopy [--no-deps]
  - cmocean [--no-deps]
  # - jupyterlab [--no-deps]
  # - nb_conda_kernels [--no-deps]

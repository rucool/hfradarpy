name: hfradarpy
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - xarray
  - netcdf4
  - geopandas
  - bottleneck [--no-deps]
  - dask [--no-deps]
  - pytest [--no-deps]
  - pytest-cov [--no-deps]
  - cartopy [--no-deps]
  - cmocean [--no-deps]
  - jupyterlab [--no-deps]
  - nb_conda_kernels [--no-deps]
